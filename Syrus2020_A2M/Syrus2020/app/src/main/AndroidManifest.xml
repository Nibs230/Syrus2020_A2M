<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.anish.syrus2020">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ResultsActivity"></activity>
        <activity android:name=".rtoQuizActivity" />
        <activity android:name=".QrCodeTry" />
        <activity android:name=".MedicineOrderActivity" />
        <activity android:name=".Text_detection" />
        <activity android:name=".LoadWebViewActivity" />
        <activity android:name=".VerifyAadhar" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".HealthInsuranceActivity" />
        <activity android:name=".SignatureActivity" />
        <activity android:name=".UserchoosingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LocationFinder" />
        <activity android:name=".BottomRate" />
        <activity android:name=".BookAppointmentActivity" />
        <activity android:name=".DoctorViewProfileActivity" />
        <activity android:name=".VideoChatActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".NotificationsActivity" />
        <activity android:name=".CallingActivity" />
        <activity android:name=".FindPeopleActivity" />
        <activity android:name=".ContactsActivity" />
        <activity android:name=".RegistrationActivity" />
        <activity android:name=".HospitalProfileActivity" />
        <activity android:name=".ChatbotActivity" />
        <activity android:name=".TryActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".DriverMapActivity"
            android:label="@string/title_activity_driver_map" />
        <activity android:name=".CompareActivity" />
        <activity android:name=".MainActivity" />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
    </application>

</manifest>